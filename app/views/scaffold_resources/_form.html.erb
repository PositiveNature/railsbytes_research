<%= form_with(model: scaffold_resource, local: true) do |form| %>
  <% if scaffold_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scaffold_resource.errors.count, "error") %> prohibited this scaffold_resource from being saved:</h2>

      <ul>
        <% scaffold_resource.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
